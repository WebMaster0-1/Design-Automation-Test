import { Meta } from '@storybook/blocks';

<Meta title="Elements" />

# Elements

This section contains atomic design elements — reusable building blocks that sit between design foundations and full components.

Add your element stories here by placing them under the `Elements/` title in your `.stories.tsx` or `.mdx` files.

import { Meta } from '@storybook/blocks';

<Meta title="MCP" />

# Model Context Protocol

This project utilizes advanced model context bindings (MCP). This documentation section will outline the internal tooling APIs exposed to AI assistants that interact with the `@fmdq/ui` codebase.

*(Further documentation regarding MCP endpoints and tool descriptors will be populated here.)*
